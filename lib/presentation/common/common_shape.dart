import 'package:flutter/material.dart';

class CommonShape {
  static var miniRoundedShape = RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(8)
  );

  static var smallRoundedShape = RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(12)
  );

  static var bigRoundedShape = RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(24)
  );
}
